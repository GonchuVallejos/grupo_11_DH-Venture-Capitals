<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/metaDatos') %>
    <link rel="stylesheet" href="/css/productDetail.css">
    <title>Detalle de Producto</title>
</head>
<body>
    <%- include('./partials/header') %>
    <link rel="stylesheet" href="/css/style.css">
    
    <main>
        <section class="product-detail">
            <div class="product-details-container">
                <div class="img-container">
                    <div class="img-container-img">
                        <img src="/img/portada/<%= productoSeleccionado.image %>" alt="<%= productoSeleccionado.name %>" class="img">
                    </div>
                    
                    <div class="thumbnail-container">
                        <img src="/img/portada/<%= productoSeleccionado.image %>" alt="Thumbnail 1" class="thumbnail">
                        <img src="/img/portada/<%= productoSeleccionado.image %>" alt="Thumbnail 2" class="thumbnail">
                        <img src="/img/portada/<%= productoSeleccionado.image %>" alt="Thumbnail 3" class="thumbnail">
                    </div>
                </div>
            </div>
            <div class="text-container">
                <div class="nameProduct">
                    <h2><%= productoSeleccionado.name %></h2>
                </div>
                <div class="details">
                    <div class="price">
                        <p>Medios de pago</p>
                    </div>
                    <div class="shipping">
                        <p>Medios de envío</p>
                    </div>
                    <div class="price-add-to-cart">
                        <p>Precio: $<%= productoSeleccionado.price %></p>
                    </div>
                    <div class="add-to-cart">
                        <button>Agregar al carrito</button>
                    </div>
                    <div>
                        <form action="/products/productEdit/<%= productoSeleccionado.id %>" method="GET" >
                            <button type="submit">Editar</button>
                        </form>
                        <form action="/products/productDetail/<%= productoSeleccionado.id %>?_method=DELETE" method="POST" >
                            <button type="submit">Eliminar</button>
                        </form>
                        
                    </div>
                </div>
            </div>
        </section>
        
        
        <div class="specifications">
            <h3>Especificaciones del producto:</h3>
            <ul>
                <li> <%= productoSeleccionado.requirement %> </li>
            <br>
            <p>Historia:</p>
            <p> <%= productoSeleccionado.history %></p>
        </div>

    </main>
    <%- include('./partials/footer') %>
    
</body>
</html>